<파일 읽는 명령어>
cat 파일명 - 파일을 읽는 명령어
vi /etc/passwd
cat /etc/passwd

cat으로 읽는 것은 텍스트 데이터만 가능. 이진 데이터는 깨져서 나오는데 여기에서 필요한것이
file 파일명 - 파일의 정보를 확인 가능

ELF - exe랑 비슷한 느낌

more 파일명 - 위에서부터 차례대로 일부분씩 보여줌
나가고 싶으면 q