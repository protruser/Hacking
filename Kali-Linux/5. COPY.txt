COPY

cp [src] [dst]
ex ) 	cp cp_test ./Desktop

cp cp_test ./Desktop/copy_test
-> cp 명령어는 특정 파일을 원하는 목적지에 특정 이름으로 복붙저장 할 수 있음

mkdir 디렉토리명
-> 새로운 디렉토리가 생성됨

cp -r normal_dir ./copy_dir
-> 디렉토리를 복사하려면 cp 뒤에 -r을 붙여야함
