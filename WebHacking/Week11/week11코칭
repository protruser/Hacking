10 주차 공부!

> XSS
피해자의 컴퓨터에서 임의의 스크립트를 삽입하는 공격
> 브라우저

* 어떻게 찾는지.

POC : alert(1), confirm
console.log();


계정 탈취.
| 쿠키에 있는 세션 ID를 탈취
httponly 속성.

Set-Cookie : httponly;

document.cookie를 칠때, httponly로 설정해놓은 cookie는 알 수 없음
그러면 어떤 방법을 취해야할까?

XSS
- 피싱사이트 유도
- 키로거 삽입

xss ->
> Stored XSS : 게시판 글 읽기.
- 키로거 삽입.
> XSS 발생하는 페이지에서, 아이디/비밀번호.

로그인 페이지.에서 취약점이 발견되었다?
여기에서는 당연히 키로거를 삽입해도 된다.


XSS
- 피싱사이트 유도

> alert('로그인이 필요한 페이지입니다.')
location.href='http://attacker.com/login.php';

************* 피싱 페이지도 한번 만들어보세요

> meta tag




| 정보 탈취
자바스크립트로 백그라운드에서 돌아서 순식간에 이 사람의 마이페이지나 그런 곳으로 들어가서 개인정보를 공격자 서버로 보내는 작업을 할거임

> javascript 이용해서 데이터를 추출하기!
.

>> 연구 보고서

form page
process page
view page

<> -> &lt; %gt;

보고서에 시행착오를 적어도 좋지만,
이왕이면 결론이 난 시행착오를 적는게 좋다

%253e -> 이중 url 인코딩

| ** 웹 개발

> 이번 주차 보고서 쓰지맙시다.