
* 버그헌팅.
| 모의해킹

* 취약점이 있는지 없는지 모른다.

* 앱 + 보안 솔루션.

* 양호 보고서

> 발견된 취약점 없음.

* XSS : 클라이언트 측 공격
> Stored
> Reflected : 서버 응답 script
--> GET
> DOM Based
클라이언트의 컴퓨터에서 조립되어서 스크립트가 실행이 되는 것임

--> Link.(url + param)

무엇을 할 수 있을까?
- 브라우저에서 자바스크립트로 할 수 있는 것은 모두 할 수 있음
> 쿠키 탈취 : 세션ID
document.cookie
> 리다이렉트
> 정보 탈취

* CSRF 공격
> XSS와 CSRF는 다른 공격인데 확실히 구분 지어야함

* 3개월차 어려웠던 점.
힘든 부분

*********** 이게 무엇인지 찾아보고
한계도 무엇인지 찾아보기
history.pushState( , , )

SQL Injection Point 2 3 4
http://ctf.segfaulthub.com:7777/sqli_7/
http://ctf.segfaulthub.com:7777/sqli_8/
http://ctf.segfaulthub.com:7777/sqli_9/

> 복습!
> Client Script

* 개인 웹 사이트!
> 연구! 실험! 테스트!